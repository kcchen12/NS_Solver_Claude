# NS_Solver Configuration File
# All lines starting with '#' are comments
# Format: key = value

# Grid parameters
nx = 400
ny = 400

# Domain dimensions
lx = 25.0
ly = 10.0
# Optional explicit domain bounds (if omitted, solver uses x:[0,lx], y:[0,ly])
x_min = -10.0
x_max = 30.0
y_min = -20.0
y_max = 20.0

# Physics parameters
re = 100.0

# Boundary conditions
# Face types: inflow | farfield | outflow | wall | periodic
bc_left = inflow
bc_right = outflow
bc_bottom = farfield
bc_top = farfield

# Inflow/farfield velocity components
inflow_u = 1.0
inflow_v = 0.0
initial_v_perturbation_pct = 2.0
inflow_w = 0.0
# Farfield behavior (used where face type is 'farfield')
# farfield_mode: dirichlet | neumann
farfield_mode = neumann

# Wall behavior (used where face type is 'wall')
# wall_slip_mode: no-slip | free-slip
wall_slip_mode = no-slip
# If true, allows non-zero normal velocity at wall boundaries
wall_penetration = false
wall_normal_velocity = 0.0

# Outflow behavior (used where face type is 'outflow')
# outflow_mode: convective | zero-gradient
outflow_mode = convective
outflow_speed = 1.0

# Time integration
t_end = 100.0
cfl = .5

# I/O parameters
save_dt = 0.02
outdir = output
# Grid generation (used by both main.py and pre_generate_grid.py)
uniform_grid = false
# Nonuniform grids use a uniform central core with tanh-stretched outer regions.
# Set <=0 in a direction to make that direction uniform.
grid_beta_x = 2.0
grid_beta_y = 2.0

grid_uniform_x_start = -2.0
grid_uniform_x_end = 15.0
grid_uniform_y_start = -5.0
grid_uniform_y_end = 5.0

# Features (true/false)
cylinder = true
verbose = true

# Cylinder
cylinder_center_x = 0.0
cylinder_center_y = 0.0
# Cylinder radius in physical units (set > 0 to override default ly/8)
cylinder_radius = 1.0

# Cylinder experiment controls
# cylinder_experiment options:
#   none
#   top-indent
#   simulated-sweeping-jet
#   geometry-resolved-sweeping-jet
cylinder_experiment = simulated-sweeping-jet

# Advanced geometry/actuation labels (used when cylinder_experiment = none)
# cylinder_geometry_mode options:
#   circle
#   circle-with-top-indent
cylinder_geometry_mode = circle
# actuator_model options:
#   none
#   sweeping-jet
#   geometry-resolved-sweeping-jet
actuator_model = sweeping-jet

# Experimental geometry controls
cylinder_indent_width = 0.0
cylinder_indent_depth = 0.0

# Experimental sweeping-jet controls
sweeping_jet_velocity_ratio = 0.25
sweeping_jet_frequency = -1.0
sweeping_jet_center_deg = 90.0
sweeping_jet_slot_width_deg = 18.0
sweeping_jet_slot_depth = 0.15
sweeping_jet_angle_deg = 25.0
sweeping_jet_phase_deg = 0.0

# Experimental geometry-resolved sweeping-jet controls
resolved_jet_cavity_width = 0.9
resolved_jet_cavity_height = 0.75
resolved_jet_slot_width = 0.24
resolved_jet_slot_height = 0.18
resolved_jet_feed_width = 0.45
resolved_jet_feed_height = 0.22
# If true, re is interpreted as Re_D based on cylinder diameter
re_is_cylinder_based = true

# Cylinder rotation
#   stationary  -> fixed_cylinder
#   constant    -> one-direction constant angular velocity
#   oscillatory -> back-and-forth sinusoidal angular velocity
cylinder_rotation_mode = stationary
# omega(t) = amplitude  for constant
# omega(t) = amplitude * sin(2*pi*frequency*t + phase) for oscillatory
cylinder_rotation_amplitude = 5.0
cylinder_rotation_frequency = 0.12
cylinder_rotation_phase_deg = 0.0
