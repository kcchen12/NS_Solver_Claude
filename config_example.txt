# NS_Solver Example Configuration File
# Based on the current working setup, with a longer end time for reference.

# Grid parameters
nx = 128
ny = 64

# Domain dimensions
lx = 4.0
ly = 2.0

# Physics parameters
re = 100.0

# Boundary conditions
# Face types: inflow | farfield | outflow | wall | periodic
bc_left = inflow
bc_right = outflow
bc_bottom = farfield
bc_top = farfield

# Inflow/farfield velocity components
inflow_u = 1.0
inflow_v = 0.001
inflow_w = 0.0

# Wall behavior (used where face type is 'wall')
# wall_slip_mode: no-slip | free-slip
wall_slip_mode = no-slip
# If true, allows non-zero normal velocity at wall boundaries
wall_penetration = false
wall_normal_velocity = 0.0

# Outflow behavior (used where face type is 'outflow')
# outflow_mode: convective | zero-gradient
outflow_mode = convective
outflow_speed = 1.0

# Time integration
t_end = 150
cfl = 0.5

# I/O parameters
save_dt = 0.25
outdir = output

# Features (true/false)
cylinder = true
# Optional cylinder center in physical units.
# If omitted, defaults to (lx/4, ly/2).
# cylinder_center_x = 1.0
# cylinder_center_y = 1.0
# Cylinder radius in physical units (set > 0 to override default ly/8)
cylinder_radius = 0.5
# If true, re is interpreted as Re_D based on cylinder diameter
re_is_cylinder_based = true
# Aerodynamic post-processing normalization (used by analyze_aerodynamics.py)
# Set >0 to force characteristic length L in St=fL/U and coefficient scaling.
# Example for legacy normalization: aero_length_scale = 0.5
# aero_length_scale = -1
# If true, use L = ly/4 for post-processing when aero_length_scale is not set.
# aero_use_cylinder_diameter = false
plot = true
verbose = true
